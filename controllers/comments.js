const { error } = require("console");
const postModel = require("../models/comments_model");
const posts_model = require("../models/comments_model");

const getComment=0

const createComment = 0

const updateComment = 0

const deleteCommentById = 0

module.exports={
    getComment,
    createComment,
    updateComment,
    deleteCommentById
};